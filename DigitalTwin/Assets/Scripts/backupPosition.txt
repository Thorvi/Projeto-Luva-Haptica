using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Position : MonoBehaviour
{
    // Start is called before the first frame update
    public ReceiverTrackingData receiver;
    public Material material;

    Material materialAux;
     
    void Start()
    {
        materialAux = material;

        transform.position = new Vector3(0f, 0f, 0f);
       
    }

    // Update is called once per frame
    void Update()
    {
        
        Debug.Log(receiver.allPositions[1]);}

        /*float x = float.Parse(receiver.allPositions[0]) / -50f;
        float y = float.Parse(receiver.allPositions[1]) / -50f;
        float z = float.Parse(receiver.allPositions[2]) / -30f;

        Vector3 pos = new Vector3(x, y, transform.position.z);
        transform.position = Vector3.Lerp(transform.position, pos, 100000f * Time.deltaTime);

        Pinca();
    }

    void Pinca() {

        float indicador_x = float.Parse(receiver.allPositions[3]) / -50f;
        float indicador_y = float.Parse(receiver.allPositions[4]) / -50f;
        float indicador_z = float.Parse(receiver.allPositions[5]) / -50f;

        float dedao_x = float.Parse(receiver.allPositions[6]) / -50f;
        float dedao_y = float.Parse(receiver.allPositions[7]) / -50f;
        float dedao_z = float.Parse(receiver.allPositions[8]) / -50f;

        float diferenca_x = Math.Abs(indicador_x - dedao_x);
        float diferenca_y = Math.Abs(indicador_y - dedao_y);

        if(diferenca_x < 10 && diferenca_y < 10) {
            material.color = Color.Lerp(material.color, Color.green, 50);
        }
        else {
            material.color = materialAux.color;
        }


    }
*/

    
}
